{% extends 'base.html.twig' %}

{% block title %}Formulaire produit{% endblock %}

{% block body %}

<h1 class="text-center mb-4">Ajouter  un produit</h1>

<div class="container">

    {{ form_start(form) }}

        {{ form_widget(form) }}
<div class="text-end">
    <button class="btn btn-success mt-3">
        Enregistrer
    </button>
</div>

    {{ form_end(form) }}

</div>
<div class="text-center mt-3">
    <a href="{{ path('admin_product_index') }}" class="btn btn-danger">
        Retour
    </a>
</div>
{% endblock %}